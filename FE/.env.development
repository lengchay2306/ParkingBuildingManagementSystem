# Dev: để trống để gọi /api/** qua Nitro proxy (cùng origin localhost → không bị CORS).
# Proxy target mặc định: Render (xem vite.config.ts). Muốn dùng BE local:
# VITE_PROXY_TARGET=http://localhost:3000
VITE_API_BASE_URL=
