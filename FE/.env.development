# Local dev: leave the API base empty so requests use relative "/api/..." paths
# and go through the Vite dev proxy (see vite.config.ts). This keeps the browser
# same-origin with localhost and avoids the backend CORS restriction.
# Production (Vercel) sets VITE_API_BASE_URL to the real backend URL instead.
VITE_API_BASE_URL=
